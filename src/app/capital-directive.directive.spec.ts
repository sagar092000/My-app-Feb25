import { CapitalDirectiveDirective } from './capital-directive.directive';

describe('CapitalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
